
apply plugin: 'java'

apply plugin: 'war'

war {
	archiveName 'myalchemyapp.war'
}

repositories {
    mavenCentral()
}

dependencies {
	compile 'javax.servlet:javax.servlet-api:3.0.1'
	compile 'org.apache.clerezza.ext:org.json.simple:0.4'
}

